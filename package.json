{
  "name": "traffic-redirect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Monetized redirect links — always-on server with file store (Railway/VPS)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  }
}
